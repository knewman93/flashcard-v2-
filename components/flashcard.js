import { auth,firestore } from '../lib/firebase';

export default function FlashCard(tester){
    return(
        <div>
            {console.log(tester)}
            <p>hey</p>
        </div>
    )
}

